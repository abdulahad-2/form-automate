# .env.example
# Copy this to .env and fill values (don't commit .env to git)

# Email configuration
ADMIN_EMAIL=admin@example.com  # Email to receive form submissions
FROM_EMAIL=your-email@gmail.com  # Must be a Gmail address

# For local development only (not needed in production)
# GMAIL_CREDENTIALS={"installed":{"client_id":"...","project_id":"...",...}}
# GMAIL_TOKEN=base64_encoded_token_here
